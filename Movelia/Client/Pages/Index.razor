@page "/"

@inject IRepository repository
<div>
    <h3>Movie</h3>
    <!-- here I invoke the MoviesList component and I set the list
    movies as the parameter that Movies is in the component -->
    <MoviesList Movies="movies"/>
    
</div>

<button @onclick="AddNewMovie">
    Add new movie
</button>

@code
{
    private List<Movie> movies;
    protected override void OnInitialized()
    {
        movies = repository.GetMovies();
    }
    private void AddNewMovie()
    {
        movies.Add(new Movie() {Title = "Movie Sample", ReleaseDate = DateTime.Today });
    }   
}
